version: '3.8'

services:
  football_intel:
    build: .
    container_name: football_intel_app
    ports:
      - "8501:8501"
    volumes:
      - ./data:/app/data
      - ~/.football_intel:/root/.football_intel
    env_file:
      - .env
    restart: unless-stopped
